import { OrdenarMensajesPipe } from './ordenar-mensajes.pipe';

describe('OrdenarMensajesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarMensajesPipe();
    expect(pipe).toBeTruthy();
  });
});
