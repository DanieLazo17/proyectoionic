import { OrdenarDestinosPipe } from './ordenar-destinos.pipe';

describe('OrdenarDestinosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarDestinosPipe();
    expect(pipe).toBeTruthy();
  });
});
