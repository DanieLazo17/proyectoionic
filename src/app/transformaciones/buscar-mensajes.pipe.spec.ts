import { BuscarMensajesPipe } from './buscar-mensajes.pipe';

describe('BuscarMensajesPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarMensajesPipe();
    expect(pipe).toBeTruthy();
  });
});
